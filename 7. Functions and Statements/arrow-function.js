// Arrow function statement body
const sum = (a, b) => {
    const result = a + b;

    return result;
}

// Arrow function expression body

// Това и горното са едни и същи.
const sum2 = (a, b, name, age, tra, lala) => a + b;

// Expression body arrow function with single parameter
const double = num => num * 2;

